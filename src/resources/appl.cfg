AppConfig {
  appPort = 9001
  DbConfig {
    dbHost = "$(DB_HOST)"
    dbName = "$(DB_NAME)"
    user = "$(DB_USER)"
    password = "$(DB_PASSWORD)"
    dbPort = "$(DB_PORT)"
    noOfStripes = 2
    idleTime = 60
    stripeSize = 10
  }
}
